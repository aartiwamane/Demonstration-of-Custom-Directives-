import { SucessDirective } from './sucess.directive';

describe('SucessDirective', () => {
  it('should create an instance', () => {
    const directive = new SucessDirective();
    expect(directive).toBeTruthy();
  });
});
